SGSearch is a simple frontend for find and grep file search tools

By default, the SlackBuild bulding the gtk3 interface. If you want 
instead to build the gtk2 interface pass GTK2=yes to SlackBuild.
